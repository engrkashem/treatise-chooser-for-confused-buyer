import React from 'react';
import './Cart.css'

const Cart = ({ book: { id, name, img, author, price } }) => {

    return (
        <div>
            <div>

            </div>

        </div>
    );
};

export default Cart;